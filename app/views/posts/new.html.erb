
<div class="content">
    <h3>Create Post</h3>
    <%# <%= form_for @post, url: user_posts_path(user_id: current_user.id) do |f| %> %>
        <%= render partial: 'form', locals: {post: @post} %>
    <%# <% end %> %>
</div>